//
//  main.cpp
//  FilesAndSorting
//
//  Created by Taylor Dunn on 9/3/20.
//  Copyright © 2020 Taylor Dunn. All rights reserved.
//

#include <iostream>
#include <fstream>

//file streams ~ cin/cout
//std::ofstream outs("someFile.txt");
//output file, variable name, file to write to
//outs << "Hello world" << std::endl;
//writes Hello Worlds into file
//input file
//ifstream ins("someFile.txt");
//std::string hello,world;
//ins >> hello >> world;
//"paths" file name and location
//someFile.txt //where is it?
//in the "working directory" is the "relative path". relative to the working directory
//someFolder/someFile.txt
//int x;
   //while (ins >> x){
   //    use(x);
   //}
   //true if read was successful, false otherwise;
   //ins.good() did the last time I use >> work?
   //std::ofstream outs("uniqueLongFileNmae.txt");
   //outs << "contents of the file" <<std::endl;
   //mdfind - name
   //product scheme edit scheme
   //std::ifstream ins("aFileThatDoesntExist.txt");
   //std::string s;
   //ins>> s;
   //std::cout << s << ins.good() << std::endl;
   //this is showing us that ins.good() was 0 so it didn't work

   //sorting. useful. complicated enough to be interesting
   //comparison, swapping
//selection sort
//for loop for i=0... st size
//find the ith smallest value
//swap with element i
//find the smalles from slot i to the end
//buble sort

//return the index of the smallest
//int findSmallest(vector<Card> cards, int start){
//    int minLocation = start;
//    for (i = start; i < cards.size(); i++){
//        if (cards[i].rank < cards[minLocation].rank){
//            minLocation = i;
//        }
//    }
//    return i;
//}

int main(int argc, const char * argv[]) {
//    for ( i = 0, i < size,  i++){
//        smallest = findSmallest(cards, i);
//        swap(i, smallest);
//    }

    
    
    return 0;
}
